package use_case.LoggedIn.add_exercise;

/**
 * Interface defining the input boundary for adding exercise.
 */
public interface AddExerciseInputBoundary {
    public void execute(AddExerciseInputData inputData);
}
